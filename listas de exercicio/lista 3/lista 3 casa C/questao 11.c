#include <stdio.h>

int main(){
int n, r;
int contador = 0;
    printf("Digite um numero inteiro qualquer para saber sua tabuada:");
    scanf("%d", n);
    while (contador <= 10){
        r = contador * n;
        printf("%d x %d = %d", n, contador, r);
        a++;
    }
}